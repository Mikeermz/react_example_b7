import React from 'react';
import MovieCard from '../../Card';
import './style.css';

const Body = ({propImage, title}) => (
  <div className="App-body">
    <MovieCard />
  </div>
);

export default Body;